{% extends 'base.html.twig' %}

{% block title %}Edit Lecturer{% endblock %}

{% block body %}
    <h1>Edit Lecturer</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Update Lecturer</button>
    {{ form_end(form) }}

    <a href="{{ path('lecturer_index') }}">Back to Lecturer List</a>
{% endblock %}
